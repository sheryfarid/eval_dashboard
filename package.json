{
  "name": "eval-report",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postinstall": "node -e \"try{require('child_process').execSync('chmod +x node_modules/.bin/vite')}catch(e){}\""
  },
  "installConfig": {
    "allowScripts": {
      "esbuild": true
    }
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
